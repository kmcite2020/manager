name: manager
description: "Global State Manager"
version: 0.0.1
homepage:
environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  package_info_plus: ^5.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
